/**
 * Providing a map storing old spus and new spus.
 */
export default class Spuses {
    /**
     * =====TYPES=====
     * %string
     */
    static pairs = new Map([
        ['achievement %string', " !>Achievements have been removed in favor of advancements."],
        ['%string', '%0']
    ])
}
